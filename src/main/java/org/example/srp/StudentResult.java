package org.example.srp;

public class StudentResult {
    public void calculateStudentResult() {
        Student student=new Student();
        student.setAveragePrice(student.getAveragePrice());
    }
}
